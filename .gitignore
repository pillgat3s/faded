# local, machine-specific build settings (signing identity)
local.mk

# build outputs
build/
app/build/
bridge/build/
app/Faded.xcodeproj/
*.dSYM/

# xcode
xcuserdata/
*.xcuserstate
DerivedData/
.DS_Store

# moved to github.com/pillgat3s/faded-tabs; an untracked copy may linger locally
extension
